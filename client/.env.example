# Leave blank for local development (Vite proxy handles /api -> localhost:5000).
# In production, set this to your deployed backend's base URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
